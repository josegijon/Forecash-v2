{
    "name": "forecash",
    "private": true,
    "workspaces": [
        "frontend",
        "core"
    ],
    "dependencies": {
        "recharts": "^3.7.0",
        "zustand": "^5.0.11"
    },
    "scripts": {
        "build": "npm run build -w core && cd frontend && npm install && npm run build"
    }
}